defmodule Unchained.Function.Local do
  # @moduledoc """Runs local commands on the host machine."""
  # use Unchained.Function

  # Come back after general function type evalauted

  # Validate input and output
  # def validate(data, schema) do
  # end

  # def run(function, args, schema \\ nil) do
  # Run the function with the given args
  # Run validation using schema changeset for input and outputs
  # args
  #   |> validate(schema)
  #   |> function
  # end

  # def supervise() do
  # end
end
